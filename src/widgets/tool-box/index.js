import config from './config'
import component from './toolBox'

/**
 * 组件入口
 **/
export default {
  // 组件名称（英文）
  name: 'tool-box',
  // 组件名称（中文）
  alias: '实列组件',
  // 组件
  component,
  // 配置
  config
}
