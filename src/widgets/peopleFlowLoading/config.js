/**
 * 组件配置
 */
export default {
  // 组件的数据模型
  model: { },
  // 组件的配置
  settings: {
    // 组件的可共享数据
  },
  // 组件的可共享数据
  state: {},
  // 组件监听的事件
  on: [
    {event:'isLoading',label:'触发人流反演加载',description: '触发人流反演加载'}

  ],
  // 组件触发的事件
  emit: [

  ],
  // 组件的大小
  size: {w: 200, h: 200, isResize: true}
}
